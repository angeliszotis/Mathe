package com.example.mathapp.ui.exam.result

/*
@AndroidEntryPoint
    class ResultFragment : BaseFragment<FragmentNavBinding>() {

       val viewModel: ExamViewModel by viewModels()
        private val navController: NavController by lazy { findNavController() }

    val : Int = requireArguments().getInt("totalQuestions", 0)
    val : Int = requireArguments().getInt("numCorrectAnswers", 0)
    val : Int = requireArguments().getInt("remainingTime", 0)



    private val totalQuestions: ResultFragmentArgs by navArgs()
    private val numCorrectAnswers: ResultFragmentArgs by navArgs()
    private val remainingTime: ResultFragmentArgs by navArgs()

    override fun getViewBinding(): FragmentNavBinding =
            FragmentNavBinding.inflate(layoutInflater)

        override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
            super.onViewCreated(view, savedInstanceState)
            binding.composeView.setContent {
                ResultScreen(totalQuestions, numCorrectAnswers, remainingTime, viewModel = viewModel)
            }
        }
    }


 */